/**
 * Banner Initialization - Must be loaded in <head> before banner HTML
 * Banner will now show on every page load
 */
(function() {
  // Banner is always visible on page load
  // Close functionality handled in banner-close.js
})();
